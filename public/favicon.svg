<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100">
  <!-- Transparent background: keep canvas clear -->
  <defs>
    <!-- Subtle glow to increase perceived weight ("bolder") in small favicon sizes -->
    <filter id="bolden" x="-50%" y="-50%" width="200%" height="200%">
      <feDropShadow dx="0" dy="0" stdDeviation="1.2" flood-color="#000" flood-opacity="0.35"/>
    </filter>
  </defs>

  <text x="50" y="50" font-size="96" text-anchor="middle" dominant-baseline="central"
        style="font-family: 'Segoe UI Emoji','Apple Color Emoji','Noto Color Emoji',sans-serif;"
        filter="url(#bolden)">🌀</text>
</svg>
