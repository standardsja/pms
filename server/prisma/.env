# Copy this file to .env and fill in your MySQL connection details.
# Example MySQL connection URL (replace user/password/host/db):
# DATABASE_URL="mysql://dev:devpass@127.0.0.1:3306/pms_dev"

# If you prefer to run MySQL in Docker for local development, you can use:
# docker run --name pms-mysql -e MYSQL_ROOT_PASSWORD=rootpass -e MYSQL_DATABASE=pms_dev -e MYSQL_USER=dev -e MYSQL_PASSWORD=devpass -p 3306:3306 -d mysql:8.0

# Then set:
# DATABASE_URL="mysql://dev:devpass@127.0.0.1:3306/pms_dev"

## NOTE: passwords containing special characters (like @ or :) must be URL-encoded
## The original password contained '@' which breaks the URL parser. Replace '@' with '%40'.
## NOTE: passwords containing special characters (like @ or :) must be URL-encoded
## The original password contained '@' which breaks the URL parser. Replace '@' with '%40'.
## Using the local `root` account for dev. Password contains '@' so it's URL-encoded as %40.
DATABASE_URL="mysql://root:F%40mily4Ever124@127.0.0.1:3306/pmsdb"

