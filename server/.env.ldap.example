# LDAP Configuration for Active Directory

# LDAP Server URL (use ldap:// for non-SSL or ldaps:// for SSL)
LDAP_URL=ldap://BOS.local:389

# Service Account DN for LDAP bind (must have read permissions)
LDAP_BIND_DN=CN=Policy Test,OU=MIS_STAFF,OU=MIS,DC=BOS,DC=local

# Service Account Password
LDAP_BIND_PASSWORD=Password@101

# Search Base DN (where to search for users)
LDAP_SEARCH_BASE=DC=BOS,DC=local

# Search Filter (use {email} as placeholder for user email)
# For Active Directory, userPrincipalName is typically the email
LDAP_SEARCH_FILTER=(userPrincipalName={email})

# Alternative search filters:
# LDAP_SEARCH_FILTER=(mail={email})
# LDAP_SEARCH_FILTER=(sAMAccountName={email})
# LDAP_SEARCH_FILTER=(|(mail={email})(userPrincipalName={email}))
